# Development Dockerfile for dashboard with hot reload
FROM oven/bun:1

WORKDIR /app

# Copy package files
COPY package.json bun.lockb ./

# Install dependencies
RUN bun install

# Expose dev server port
EXPOSE 5173

# Start dev server
CMD ["bun", "run", "dev", "--host", "0.0.0.0"]
